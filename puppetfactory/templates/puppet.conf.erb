[main]
    certname = <%= @username %>.puppetlabs.vm
    server = <%= @servername %>
    basemodulepath = /root/puppetcode/modules

[agent]
    report = true
    classfile = $vardir/classes.txt
    localconfig = $vardir/localconfig
    pluginsync = true
